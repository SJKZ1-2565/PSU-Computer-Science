name: C/C++ CI

on:
  push:
    branches: [ "1-2568(2025)" ]
  pull_request:
    branches: [ "1-2568(2025)" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Compile
      run: g++ *.cpp -o main

    - name: Run
      run: ./main